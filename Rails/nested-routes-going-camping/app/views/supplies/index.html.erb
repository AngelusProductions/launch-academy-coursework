<h1> <%= @title %> </h1>

<ul>
  <% if @supplies.length > 0 %>
    <% @supplies.each do |supply| %>
      <li><%= supply.name %></li>
    <% end %>
  <% else %>
    <h2>NOTHING</h2>
  <% end %>
</ul>

<p><%= link_to "back", campsite_campers_path(@camper.campsite_id) %></p>
